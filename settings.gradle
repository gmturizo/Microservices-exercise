pluginManagement {
    repositories {
        maven {
            url "${repository}"
        }
    }
}

rootProject.name = 'Microservices-exercise'

include 'service-cotizador'
include 'service-flota'
include 'service-vehiculo'
